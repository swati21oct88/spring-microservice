server:
  port: 0
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone:
        http://localhost:8761/eureka
  instance:
    instance-id: user-ws:${random.value}
spring:
  application:
    name: user-service